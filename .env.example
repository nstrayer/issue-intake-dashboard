# Positron Issue Intake Dashboard - Environment Configuration
#
# Most users don't need this file! The server auto-discovers AWS credentials
# from your existing configuration (SSO, credentials file, environment vars).
#
# Only create a .env file if you need to override automatic discovery.

# AWS Bedrock Configuration (usually auto-discovered)
# CLAUDE_CODE_USE_BEDROCK=1
# AWS_REGION=us-east-1
# AWS_PROFILE=my-custom-profile

# Server Configuration
# PORT=3001
